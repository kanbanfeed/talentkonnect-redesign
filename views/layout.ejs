<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | TalentKonnect</title>
    
    <link rel="stylesheet" href="/css/style.css">
    
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

    <style>
        /* Default hidden state for profile to prevent flashing */
        #header-profile-pill {
            transition: opacity 0.2s;
            cursor: pointer;
        }
        
        /* Login Button Style (In case user is logged out) */
        .nav-login-btn {
            background: #4f46e5;
            color: white;
            padding: 8px 20px;
            border-radius: 20px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            display: none; /* Hidden by default */
        }
    </style>
</head>
<body>
    
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo-section">
                <ion-icon name="cube" style="color: #818cf8; font-size: 1.5rem;"></ion-icon>
                TalentKonnect
            </a>

          <div class="nav-links" role="navigation" aria-label="Main navigation">
    <a href="/" class="nav-item">Home</a>
    <a href="/dashboard" class="nav-item">Dashboard</a>
    <a href="/apply-job" class="nav-item">Apply Job</a>
    <a href="/post-job" class="nav-item">Post Job</a>
    <a href="/messaging" class="nav-item">Messages</a>
</div>

<div class="nav-right">
    <!-- hamburger (hidden on desktop, shown on mobile by CSS) -->
    <button class="nav-toggle" aria-label="Open menu" aria-expanded="false">☰</button>

    <div class="profile-pill" id="header-profile-pill" style="display: flex; visibility: visible; opacity: 1;">
  <div class="user-info">
    <span class="user-name" id="nav-user-name">Loading...</span>
    <span class="user-role" id="nav-user-role">...</span>
  </div>
  <!-- start hidden, will be filled by JS -->
  <div class="user-avatar" id="nav-user-avatar" aria-hidden="true" style="visibility:hidden"></div>
</div>


    <a href="/login" id="header-login-btn" class="nav-login-btn">Login</a>
</div>

<!-- MOBILE NAV (toggle target) -->
<div class="mobile-nav" aria-hidden="true">
    <a href="/" class="nav-item">Home</a>
    <a href="/dashboard" class="nav-item">Dashboard</a>
    <a href="/apply-job" class="nav-item">Apply Job</a>
    <a href="/post-job" class="nav-item">Post Job</a>
    <a href="/messaging" class="nav-item">Messages</a>
</div>

        </div>
    </nav>
  
    <main class="container">
        <%- body %>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', async () => {
            
            // --- 1. ACTIVE MENU HIGHLIGHTER ---
            const currentPath = window.location.pathname;
            const navLinks = document.querySelectorAll('.nav-item');
            
            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === currentPath || (currentPath === '/' && link.getAttribute('href') === '/')) {
                    link.classList.add('active');
                }
            });

            // --- 2. HEADER DATA FETCH ---
            const sessionStr = localStorage.getItem('crowbar_session');
            const userStr = localStorage.getItem('crowbar_user');
            
            const profilePill = document.getElementById('header-profile-pill');
            const loginBtn = document.getElementById('header-login-btn');

            if (sessionStr && userStr) {
                // USER IS LOGGED IN
                loginBtn.style.display = 'none';
                profilePill.style.display = 'flex';

                const user = JSON.parse(userStr);
                const SB_URL = 'https://zfxnhbymlhvukeqmsyhj.supabase.co';
                const SB_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpmeG5oYnltbGh2dWtlcW1zeWhqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE4MTIyMTIsImV4cCI6MjA3NzM4ODIxMn0.kKB1RsLvXJuROPlBY5bWeBzTWDGGe_UTouWkfSc7RXs'; // <--- PASTE KEY HERE
                const client = supabase.createClient(SB_URL, SB_KEY);

                const { data: profile } = await client
                    .from('profiles')
                    .select('*')
                    .eq('id', user.id)
                    .single();

                if (profile) {
                    document.getElementById('nav-user-name').innerText = profile.full_name || 'User';
                    document.getElementById('nav-user-role').innerText = 'Talent';
                    const initials = (profile.full_name || 'U').charAt(0).toUpperCase();
                    document.getElementById('nav-user-avatar').innerText = initials;
                } else {
                    document.getElementById('nav-user-name').innerText = user.email.split('@')[0];
                }
            } else {
                // USER IS LOGGED OUT
                profilePill.style.display = 'none'; // Hide profile
                loginBtn.style.display = 'block';   // Show Login button
            }
        });
    </script>
    <script>
  // Mobile nav toggle
  (function(){
    document.addEventListener('DOMContentLoaded', function() {
      const navToggle = document.querySelector('.nav-toggle');
      const mobileNav = document.querySelector('.mobile-nav');

      if (!navToggle || !mobileNav) return;

      navToggle.addEventListener('click', function(e) {
        e.stopPropagation();
        const shown = mobileNav.classList.toggle('_show');
        navToggle.setAttribute('aria-expanded', shown ? 'true' : 'false');
        mobileNav.setAttribute('aria-hidden', shown ? 'false' : 'true');
      });

      // close on outside click
      document.addEventListener('click', (e) => {
        if (!mobileNav.contains(e.target) && !navToggle.contains(e.target)) {
          mobileNav.classList.remove('_show');
          navToggle.setAttribute('aria-expanded', 'false');
          mobileNav.setAttribute('aria-hidden', 'true');
        }
      });

      // prevent closing when clicking inside
      mobileNav.addEventListener('click', function(e){ e.stopPropagation(); });
    });
  })();
</script>

</body>
</html>